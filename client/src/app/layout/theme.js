import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  palette: {
    primary: {
      main: "#1C1D22",
      background: "#EEEEEE",
    },
    secondary: {
      main: "#C0C0C0", // Silver
    },
  },
});

export default theme